/* https://www.spoj.com/problems/ABCPATH/  */
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int tc = 1;
    while(true)
    {
        int w, h;
        cin >> w >> h;
        if(w == 0 && h == 0)
        {
            break;
        }

        // code here

        cout<<"Case "<<tc<<": "<<ans<<endl;
    }

    return 0;
}
